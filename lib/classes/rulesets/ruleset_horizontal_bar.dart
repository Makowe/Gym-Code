import 'ruleset.dart';

/// Ruleset for horizontal bar. Currently adds no rules beyond [RuleSet] (see
/// reqs/ruleset_horizontal_bar.rst); kept as its own class so
/// apparatus-specific rules have a place to live once the ruleset gains any.
class HorizontalBarRuleSet extends RuleSet {}
