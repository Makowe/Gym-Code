import 'ruleset.dart';

/// Ruleset for parallel bars. Currently adds no rules beyond [RuleSet] (see
/// reqs/ruleset_parallel_bars.rst); kept as its own class so
/// apparatus-specific rules have a place to live once the ruleset gains any.
class ParallelBarsRuleSet extends RuleSet {}
