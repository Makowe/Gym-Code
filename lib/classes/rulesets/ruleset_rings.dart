import 'ruleset.dart';

/// Ruleset for rings. Currently adds no rules beyond [RuleSet] (see
/// reqs/ruleset_rings.rst); kept as its own class so apparatus-specific
/// rules have a place to live once the ruleset gains any.
class RingsRuleSet extends RuleSet {}
